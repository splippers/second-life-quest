<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.slquest.viewer">

  <!-- Quest 3 requires these permissions -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
  <uses-permission android:name="com.oculus.permission.EYE_TRACKING" />
  <uses-permission android:name="com.oculus.permission.BODY_TRACKING" />
  <uses-permission android:name="com.oculus.permission.USE_SCENE" />

  <!-- OpenXR/Vulkan requirements -->
  <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" android:version="1" />
  <uses-feature android:name="com.oculus.software.overlay_keyboard" android:required="false" />

  <application
    android:label="SLQuest"
    android:icon="@mipmap/ic_launcher"
    android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
    android:allowBackup="false">

    <!-- NativeActivity: OpenXR talks directly to the display; no SurfaceView needed -->
    <activity
      android:name="android.app.NativeActivity"
      android:exported="true"
      android:screenOrientation="landscape"
      android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode"
      android:launchMode="singleTask"
      android:resizeableActivity="false">

      <!-- Points to our .NET published native library -->
      <meta-data android:name="android.app.lib_name" android:value="slquest" />

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <!-- Quest VR category -->
        <category android:name="com.oculus.intent.category.VR" />
      </intent-filter>

      <!-- OpenXR system feature declaration -->
      <meta-data android:name="com.oculus.supportedDevices" android:value="quest3" />
      <meta-data android:name="com.oculus.handtracking.frequency" android:value="HIGH" />
      <meta-data android:name="com.oculus.handtracking.version" android:value="V2.0" />
    </activity>
  </application>
</manifest>
